# Skopiuj do backend/.env.local (obok istniejącego pliku) gdy używasz PostgreSQL z docker-compose w katalogu głównym repozytorium.
# Uruchomienie bazy: docker compose up -d (z roota monorepo).

DATABASE_URL="postgresql://medical_app:medical_dev_secret@127.0.0.1:5432/medical_document?serverVersion=16&charset=utf8"
